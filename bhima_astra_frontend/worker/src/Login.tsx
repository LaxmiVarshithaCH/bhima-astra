/**
 * Legacy Login.tsx — superseded by LoginPage.tsx + CinematicSlideshow.tsx
 * Kept as historical reference only. Not used in the app.
 */
import React from 'react';

const Login: React.FC = () => <></>;
export default Login;